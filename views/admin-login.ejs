<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>
    <style>
        :root {
            --accent-color: #FFE66D;
            --black:#000;
        }
        body {
            background-color: var(--accent-color); /* Set background color to black */
            color: var(--accent-color); /* Optional: Set text color to white for better contrast */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; /* Full height for centering */
            margin: 0; /* Remove default margin to prevent scrolling */
            overflow: hidden; /* Prevent scrolling */
        }
        .card {
            background-color: var(--black); /* Semi-transparent background */
            padding: 40px; /* Increased padding for better spacing */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 300px; /* Fixed width for the card */
            text-align: center;
        }
        input {
            width: calc(100% - 20px); /* Adjust width to account for padding */
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
        }
        button {
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: var(--accent-color);
            color: black;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1><%= shopName %></h1>
        <h2>Admin Login</h2>
        <form action="/<%=shopName %>/login" method="POST" target="_blank">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required><br><br>
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" required><br><br>
            <button type="submit">Login</button>
          </form>
    </div>
    <!-- Add more admin functionalities here -->
</body>
</html>